diff --git a/src/rime/gear/memory.cc b/src/rime/gear/memory.cc
--- a/src/rime/gear/memory.cc
+++ b/src/rime/gear/memory.cc
@@ -99,7 +99,8 @@ bool Memory::DiscardSession() {
 }
 
 void Memory::OnCommit(Context* ctx) {
-  if (!user_dict_ || user_dict_->readonly())
+  if (!user_dict_ || user_dict_->readonly() || !ctx ||
+      ctx->get_option("language_input_sensitive"))
     return;
   StartSession();
   CommitEntry commit_entry(this);
@@ -120,6 +121,8 @@ void Memory::OnCommit(Context* ctx) {
 void Memory::OnDeleteEntry(Context* ctx) {
   if (!user_dict_ || user_dict_->readonly() || !ctx || !ctx->HasMenu())
     return;
+  if (ctx->get_option("language_input_sensitive"))
+    return;
   auto phrase =
       As<Phrase>(Candidate::GetGenuineCandidate(ctx->GetSelectedCandidate()));
   if (Language::intelligible(phrase, this)) {
@@ -131,7 +134,8 @@ void Memory::OnDeleteEntry(Context* ctx) {
 }
 
 void Memory::OnUnhandledKey(Context* ctx, const KeyEvent& key) {
-  if (!user_dict_ || user_dict_->readonly())
+  if (!user_dict_ || user_dict_->readonly() ||
+      (ctx && ctx->get_option("language_input_sensitive")))
     return;
   if ((key.modifier() & ~kShiftMask) == 0) {
     if (key.keycode() == XK_BackSpace && DiscardSession()) {
diff --git a/src/rime/gear/script_translator.cc b/src/rime/gear/script_translator.cc
--- a/src/rime/gear/script_translator.cc
+++ b/src/rime/gear/script_translator.cc
@@ -199,10 +199,14 @@ an<Translation> ScriptTranslator::Query(const string& input,
   DLOG(INFO) << "input = '" << input << "', [" << segment.start << ", "
              << segment.end << ")";
 
-  FinishSession();
+  bool sensitive =
+      engine_->context()->get_option("language_input_sensitive");
+  if (!sensitive)
+    FinishSession();
 
   bool enable_user_dict =
-      user_dict_ && user_dict_->loaded() && !IsUserDictDisabledFor(input);
+      user_dict_ && user_dict_->loaded() && !sensitive &&
+      !IsUserDictDisabledFor(input);
 
   size_t end_of_input = engine_->context()->input().length();
   // the translator should survive translations it creates
diff --git a/src/rime/gear/table_translator.cc b/src/rime/gear/table_translator.cc
--- a/src/rime/gear/table_translator.cc
+++ b/src/rime/gear/table_translator.cc
@@ -246,10 +246,14 @@ an<Translation> TableTranslator::Query(const string& input,
   DLOG(INFO) << "input = '" << input << "', [" << segment.start << ", "
              << segment.end << ")";
 
-  FinishSession();
+  bool sensitive =
+      engine_->context()->get_option("language_input_sensitive");
+  if (!sensitive)
+    FinishSession();
 
   bool enable_user_dict =
-      user_dict_ && user_dict_->loaded() && !IsUserDictDisabledFor(input);
+      user_dict_ && user_dict_->loaded() && !sensitive &&
+      !IsUserDictDisabledFor(input);
 
   const string& preedit(input);
   string code = input;
@@ -543,6 +547,8 @@ an<Translation> TableTranslator::MakeSentence(const string& input,
                                               bool include_prefix_phrases) {
   bool filter_by_charset = enable_charset_filter_ &&
                            !engine_->context()->get_option("extended_charset");
+  bool sensitive =
+      engine_->context()->get_option("language_input_sensitive");
   DictEntryCollector collector;
   UserDictEntryCollector user_phrase_collector;
   WordGraph graph;
@@ -555,7 +561,7 @@ an<Translation> TableTranslator::MakeSentence(const string& input,
     string active_key = active_input + ' ';
     auto& same_start_pos = graph[start_pos];
     // lookup dictionaries
-    if (user_dict_ && user_dict_->loaded()) {
+    if (!sensitive && user_dict_ && user_dict_->loaded()) {
       for (size_t len = 1; len <= active_input.length(); ++len) {
         size_t consumed_length =
             consume_trailing_delimiters(len, active_input, delimiters_);
@@ -592,7 +598,7 @@ an<Translation> TableTranslator::MakeSentence(const string& input,
           break;
       }
     }
-    if (encoder_ && encoder_->loaded()) {
+    if (!sensitive && encoder_ && encoder_->loaded()) {
       UnityTableEncoder::AddPrefix(&active_key);
       for (size_t len = 1; len <= active_input.length(); ++len) {
         size_t consumed_length =

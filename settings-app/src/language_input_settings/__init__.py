"""Language Input settings application (M1 skeleton).

Part of the Language Input project (Weasel/Rime fork). This package is a
standalone Python tray application (GUI built on PySide6 / Qt for Python); it
never compiles any C++ and only ever reads either the user's Rime data or the
Weasel installation, writing exclusively to files it owns or to explicitly
surgical edits.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
